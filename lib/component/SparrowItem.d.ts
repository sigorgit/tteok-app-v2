import { DomNode } from "@hanul/skynode";
import Sparrows from "../view/Sparrows";
export default class SparrowItem extends DomNode {
    private tab;
    private id;
    private sparrow;
    private mixAmount;
    private claimable;
    private refreshInterval;
    constructor(tab: Sparrows, id: number);
    private loadImage;
    private load;
    delete(): void;
}
//# sourceMappingURL=SparrowItem.d.ts.map