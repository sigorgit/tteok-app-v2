import { DomNode } from "@hanul/skynode";
import Sparrows from "../view/Sparrows";
export default class Sparrow extends DomNode {
    private tab;
    private id;
    private pixelCat;
    private mixAmount;
    private claimable;
    private refreshInterval;
    constructor(tab: Sparrows, id: number);
    private loadImage;
    private load;
    delete(): void;
}
//# sourceMappingURL=Sparrow.d.ts.map